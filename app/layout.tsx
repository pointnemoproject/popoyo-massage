import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Popoyo Massage & Healing | Mobile Massage in Popoyo, Nicaragua",
  description:
    "Book professional mobile massage in Popoyo, Nicaragua. Relaxation, deep tissue, surf recovery, and couples massage delivered to your villa, hotel, or rental.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
